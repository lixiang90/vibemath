# 三篇毕业论文 Goal：第三次论文组会纪要

日期：2026-09-03

## 1. 联合复现

导师在第三轮产物稳定后联合运行九组测试：

```text
PAPER_SQUARE_test.py
PAPER_SQUARE_SAFE_test.py
PAPER_SQUARE_MASK77_test.py
PAPER_CUBE_test_torsion_gate.py
PAPER_CUBE_C29_test_model.py
PAPER_CUBE_P6_test_gate.py
PAPER_ELLIPTIC_MOODY_JUYAL_TEST.py
PAPER_ELLIPTIC_NEXT_test.py
PAPER_ELLIPTIC_CAMPBELL_test.py

Ran 75 tests
OK
```

三份第三轮交叉审稿均已完成。以下“接受”仍只限于被证明的定理/证书，不包括尚未完成的
新颖性检索、全局秩或有理点分类。

## 2. 甲线：首个候选主定理通过数学审稿

交叉审稿独立确认：

```text
H: y^2=t(t+2)(t+3)(t+6)
H(Z)={(-6,0),(-3,0),(-2,0),(0,0),(6,+-72)}.
```

证明的 18 个平方自由核分支完整。15 个分支由对正确联立方程的有限剩余类穷尽排除；其余
三个分支的因子恒等式、正负号、小参数和大小矛盾均通过独立复核。这个完备性结论不依赖
百万高度搜索或 Mordell--Weil 数据。

mask 77/89 在原 98 个剩余模式中的计数为 `26+25-7=44`；逐 occurrence 以同一个
整数参数检查全部 15 个字符后，44 个模式全部排除。因此安全题严格进度为：

```text
284 -> 98（连续四字符定理） -> 54（完整 mask 77/89 积分点定理）。
```

数学审稿无阻断反例。发表前的证书修订是：把三个大小证明结构化、绑定 SAFE/Round04
输入哈希、保存剩余 54 个 ID，并让 mask77 单包自行断言每行恰有 15 个不同字符。
新颖性尚只能称候选；须继续按 S-整数点、同时 Pell 方程和同一 quartic 方程做
MathSciNet/zbMATH/作者页级查重。

第四轮任务：完成上述审计修订，建立可编译 LaTeX 论文骨架并把 651--343--284、186/98、
mask77/89 定理写成自足正文；同时在剩余 54 模式上按“每个可闭合 cover 能排除的模式数/
算术难度”选择下一代表，优先寻找同样可由 gcd+有限模闭合者。若 mask99 不满足这个条件，
不因旧计划而机械选择它。

## 3. 乙线：C29 修复通过，P6 仍是几何闸门

C29 的 `(r,k)` 与 sextic 主开集两向复合、七个分母、六个 `k=0,infinity` 分支和 raw
transvectant 不变量均通过复核。当前仍缺原始 `(r,s)<->(r,k)` 的完整两向证书及全部
例外纤维；因此可接受主开集双有理模型，但不能把“完整正规化点对应”写成已闭合。

`P_6(4)` 的 AP 归约、零/无穷远边界、2/3/5 必要类、六个 genus-1 商模型和五个好素数
迹恒等正确。有限个迹只用于防错，不能证明 Q-同源；完整商态射、总商 conic、
Kani--Rosen 同源映射和第四幂提升覆盖均未落盘。

第四轮任务：先把 C29 原始 `(r,s)` 入口与例外纤维补齐并冻结；主力转 P6，给六个商写
完整态射和 Kani--Rosen 分解证明，再用公开数据库只作选线，选择 C1/C2 中严格秩门槛较低
的一条建立有限 covering collection。没有本地审计 CAS 时，不把数据库秩当论文证明。

## 4. 丙线：全部局部格闭合，具体 torsor 投影确定

第三轮严格完成：

```text
64 rows x 8 places = 512 cells;
384 YES, 128 NO, 0 UNRESOLVED;
old 56 unresolved = 24 YES at p=3 + 32 NO at p=59 or 71699;
ambient survivors = 8 on E side + 4 on E' side.
```

估值正规化证明与逐格证书通过独立复核。进一步严格重建 `H=g_m(8)`，计算二进四次的
`I,J`、三次 étale 预解式、Cassels 类 `z(H)` 的平方范数与 `u=64` 缩放，确定其有理
2-挠投影恰为 E 侧 `d=35`。报告正确区分这一投影和完整 `H^1(Q,J_H[2])` 类，没有把
单个 d 当作完整 torsor。

非阻断缺口：二次数域分量仍须结构化进证书；必须加入“好素数处自动局部可解”的标准引理
及来源，并交叉引用 `C_H` 处处局部可解的原始证书。补齐后应判断 `8+4` 是否可严格升级为
两侧 2-isogeny Selmer 群，而非继续保守称 ambient 集。

第四轮任务：完成上述升级条件；计算两侧 Selmer 群与已知 Mordell--Weil 像的差，并对
含 `d=35` 的完整类建立 Cassels--Tate 配对/完整 2-cover descent 入口。目标是证明
`C_H` 类非平凡或明确给出剩余的单个全局阻断，不能停在更多局部表。

## 5. Goal 状态

Goal 保持 **ACTIVE**。甲已有一个经数学交叉审稿的候选主定理，但尚无完整论文、新颖性
核查和投稿材料；乙、丙都没有完成全局有理点结论。第四轮开始同时推进“主定理加固”和
论文写作，不等到所有开放分支解决后才组织正文。
