# Submission materials — squareclass patterns

Status: mathematically submission-ready repository package; no actual journal
submission is requested or claimed.

Current mathematical snapshot: the mask-85 gate leaves 2 necessary patterns;
the squareclasses suite has 96 tests. Supplement release
`paper-square-supplement-v0.10.1` is bound by SHA-256
`deb3eade7c9f25c6e0c8da019f21f7a0943bdd50fcf263f7add6ed8b3ed0309e`.
Realizability of every survivor and `R_2(7)` remain open.

The mathematics lives in
the sibling `paper/`, `code/`, `certificates/`, `mathematical-notes/`,
`reviews/`, and `reproducibility/` directories.  This directory deliberately
contains only editable submission prose.

The current public working copy is
<https://github.com/lixiang90/vibemath/tree/main/square-progressions/seven-consecutive-squareclasses>.
The repository-root `MANIFEST.sha256` still records the Round10 tree and is not
the current Round11 byte inventory.  It must be regenerated only when the
Round11 payload is frozen.  The paper-specific supplement manifest currently
binds the 38 core mathematical artifacts through mask 85, but it is not a
self-updating description of every later review or submission file.

Contents:

- `abstract.txt`: submission-form abstract;
- `cover_letter.md`: optional venue-neutral letter template;
- `journal_shortlist.md`: scope-based venue candidates;
- `author_contributions.md`: CRediT-style record for Codex (GPT-5.6-sol);
- `data_code_availability.md`: public working URL and immutable-release gate;
- `limitations.md`: exact claim boundary;
- `USER_INPUT_CHECKLIST.md`: metadata needed only if an actual external
  submission is later attempted.

The sole named author is `Codex (GPT-5.6-sol)`. No affiliation, e-mail address,
ORCID, funding source or competing-interest statement is invented. Such
venue-specific fields and an immutable release locator may be supplied later
if an actual external submission is requested; they are not completion gates
for the present submission-ready mathematical package.
