# Submission materials — squareclass patterns

Status: draft prose, not authorized for submission.  The mathematics lives in
the sibling `paper/`, `code/`, `certificates/`, `mathematical-notes/`,
`reviews/`, and `reproducibility/` directories.  This directory deliberately
contains only editable submission prose.

The current public working copy is
<https://github.com/lixiang90/vibemath/tree/main/square-progressions/seven-consecutive-squareclasses>.
`MANIFEST.sha256` at repository root is the current byte inventory.  The older
paper-specific supplement manifest is retained as a historical release record;
it is not a self-updating description of every later audit file.

Contents:

- `abstract.txt`: submission-form abstract;
- `cover_letter.md`: venue-neutral letter with identity and policy gates;
- `journal_shortlist.md`: scope-based venue candidates;
- `author_contributions.md`: CRediT-style draft requiring human approval;
- `data_code_availability.md`: public working URL and immutable-release gate;
- `limitations.md`: exact claim boundary;
- `USER_INPUT_CHECKLIST.md`: facts and author approvals still required.

Before submission, the authors must select an eligible journal, independently
verify the proof and references, approve the AI disclosure in the manuscript,
replace every identity/funding/conflict placeholder, and freeze the exact
payload under an immutable URL or DOI.  No placeholder may be invented.
